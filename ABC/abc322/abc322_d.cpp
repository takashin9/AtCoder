#include <bits/stdc++.h>
using namespace std;
#if __has_include(<atcoder/all>)
    #include <atcoder/all>
using namespace atcoder;
#endif

using ll = long long;



int main() {
    vector<vector<string>> p(3, vector<string>(4));
    for (int i = 0; i < 3; i++) {
        for (int j = 0; j < 4; j++) {
            cin >> p[i][j];
        }
    }
    vector<vector<vector<string>>> pi(3, vector<vector<string>>(4, vector<string>(4)));
    for (int i = 0; i < 3; i++) {
        for (int j = 0; j < 4; j++) {
            
        }
    }
    
    
    return 0;
}