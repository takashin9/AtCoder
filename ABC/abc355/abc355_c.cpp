#include <bits/stdc++.h>
using namespace std;
#if __has_include(<atcoder/all>)
    #include <atcoder/all>
using namespace atcoder;
#endif

using ll = long long;


int main() {
    int n, t; cin >> n >> t;
    vector<int> a(t), c(n, n), r(n, n);
    for (int i = 0; i < t; i++) cin >> a[i];
    
    return 0;
}